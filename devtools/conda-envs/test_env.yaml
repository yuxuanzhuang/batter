name: test
channels:

  - defaults
dependencies:
  # Base depends
  - python
  - pip
  - MDAnalysis
  - MDAnalysisTests
  - loguru
  - pydantic
  - click
  ## Bat.py
  - openbabel
  - ambertools
  - openmm
  - openmmtools
  
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

