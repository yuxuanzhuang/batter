name: test
channels:
  - defaults
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - MDAnalysis
  - MDAnalysisTests
  - loguru
  - pydantic
  - click
  ## Bat.py
  - openbabel
  - ambertools
  - openmm
  - openmmtools
  - vmd
  # issue with vmd?
  - libsqlite<3.46.0
  # Testing
  - pytest
  - pytest-cov
  - codecov

  # Pip-only installs
  - pip:
    - codecov