name: test
channels:
  - defaults
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - MDAnalysis
  - MDAnalysisTests
  - loguru
  - pydantic
  - click
  - rdkit
  ## Bat.py
  - openbabel
  - ambertools
  - openmm
  - openmmtools
  # install vmd-python instead of vmd
  - vmd-python
  # Testing
  - pytest
  - pytest-cov
  - codecov
  - pytest-snapshot

  # Pip-only installs
  - pip:
    - codecov