name: test
channels:
  - defaults
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - MDAnalysis
  - MDAnalysisTests
  - loguru
  - pydantic
  - click
  ## Bat.py
  - openbabel
  - ambertools
  - openmm
  - openmmtools
  # install vmd-python instead of vmd
  - vmd-python
  # Testing
  - pytest
  - pytest-cov
  - codecov

  # Pip-only installs
  - pip:
    - codecov